<Page navigatingTo="onNavigatingTo" xmlns="http://schemas.nativescript.org/tns.xsd">
    
    <ActionBar>
        <Label text="Home" />
    </ActionBar>

    <StackLayout>
        <Button id="btnOk" text="OK" tap="{{ onTap }}"  class="-primary"/>
        <Label id="lblMessage" text="{{ toHere }}" class="h2 text-center"/>
        <TextField id="txtWrite" text="{{ fromHere }}" style="font-size:20px"/>        
    </StackLayout>
</Page>
